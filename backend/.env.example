# Backend environment variables (example)

# Required in production
JWT_SECRET=change-me-to-a-long-random-string
VENDOR_PASSWORD=admin123

# CORS: comma-separated allowed frontend origins (Netlify URL)
# Example: https://your-site.netlify.app,https://your-custom-domain.com
CORS_ORIGINS=

# Database (choose ONE approach)

# Option A (recommended on Render): full connection string (Supabase)
# Example: postgresql://USER:PASSWORD@HOST:5432/DATABASE?sslmode=require
DATABASE_URL=

# Option B: discrete connection fields
DB_HOST=localhost
DB_NAME=smartseller
DB_USER=postgres
DB_PASSWORD=password
DB_PORT=5432

# Set true when your DB requires SSL (Supabase does)
DB_SSL=false

# Optional
DB_CONNECT_TIMEOUT_MS=5000
